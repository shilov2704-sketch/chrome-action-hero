# QA Test Recorder - Chrome Extension

Расширение для Chrome DevTools, позволяющее записывать действия пользователя, генерировать XPath локаторы и воспроизводить тесты.

## 🚀 Установка

1. Откройте Chrome и перейдите по адресу `chrome://extensions/`
2. Включите "Режим разработчика" (Developer mode) в правом верхнем углу
3. Нажмите "Загрузить распакованное расширение" (Load unpacked)
4. Выберите папку `chrome-extension` с файлами расширения
5. Расширение появится в списке установленных

## 📖 Использование

1. Откройте любую веб-страницу
2. Откройте Chrome DevTools (F12)
3. Найдите вкладку "QA Recorder" в DevTools
4. Нажмите "Создать запись"
5. Введите название записи
6. Выберите типы селекторов (CSS, ARIA, Text, XPath, Pierce)
7. Нажмите "Начать запись"
8. Выполняйте действия на странице - они будут записаны
9. Нажмите "Остановить запись" когда закончите
10. Используйте кнопку "Replay" для воспроизведения или "Экспорт JSON" для сохранения

## ✨ Функции

- ✅ Запись действий пользователя (клики, ввод текста, навигация)
- ✅ Генерация селекторов: CSS, ARIA, Text, XPath, Pierce
- ✅ Специальная логика XPath для data-testid + текст
- ✅ Пикер элементов для ручного выбора
- ✅ Добавление WaitForElement проверок
- ✅ **Продолжение записи существующих тестов**
- ✅ Настройки воспроизведения (скорость, timeout)
- ✅ Экспорт/импорт записей в JSON
- ✅ Воспроизведение записей
- ✅ Красивый интерфейс в стиле DevTools

## 🔄 Продолжение записи

Вы можете добавлять новые шаги в уже существующие записи:

1. Откройте любую сохраненную запись из списка записей
2. Нажмите кнопку "Продолжить запись" в правом верхнем углу
3. Расширение переключится в режим записи с сохранением всех существующих метаданных и шагов
4. Выполняйте новые действия на странице - они будут добавлены к существующим шагам
5. Нажмите "Остановить запись" когда закончите
6. Запись будет автоматически обновлена и объединена с новыми шагами

**Примечания:**
- При продолжении записи автоматически пропускаются начальные шаги `setViewport` и `navigate`, так как они уже присутствуют в записи
- Все метаданные записи (название, SuiteName, селекторы) сохраняются
- Новые шаги добавляются в конец существующего массива шагов

## 📁 Структура файлов

```
chrome-extension/
├── manifest.json          # Конфигурация расширения
├── devtools.html         # Точка входа в DevTools
├── devtools.js           # Создание панели в DevTools
├── panel.html            # HTML интерфейса рекордера
├── panel.css             # Стили интерфейса
├── panel.js              # Логика UI и управления
├── content-script.js     # Запись действий на странице
├── background.js         # Фоновый скрипт
└── icons/                # Иконки расширения
    ├── icon16.png
    ├── icon48.png
    └── icon128.png
```

## 🎨 Особенности XPath генерации

Расширение использует специальную логику для генерации XPath селекторов:
- Для элементов с `data-testid` и вложенным текстом в `<span>`:
  ```xpath
  //div[@data-testid='Button_Tag_7a9741' and .//span[text()='Создать заявку']]
  ```
- Это позволяет точно идентифицировать переиспользуемые элементы по комбинации атрибута и текста

## 🔧 Разработка

Для внесения изменений:
1. Отредактируйте нужные файлы
2. Перейдите на `chrome://extensions/`
3. Нажмите кнопку "Обновить" (🔄) у расширения
4. Обновите страницу DevTools (может потребоваться закрыть и открыть заново)

## 📝 Формат экспорта JSON

Записи экспортируются в формате, совместимом с Chrome DevTools Recorder:

```json
{
  "title": "Название записи",
  "selectorAttribute": "data-testid",
  "steps": [
    {
      "type": "navigate",
      "url": "https://example.com"
    },
    {
      "type": "click",
      "selectors": [
        ["[data-testid='button']"],
        ["xpath//div[@data-testid='button' and .//span[text()='Кнопка']]"]
      ],
      "offsetX": 10,
      "offsetY": 20
    }
  ]
}
```

## ⚠️ Примечания

- Расширение работает только с открытыми DevTools
- Для записи событий страница должна быть полностью загружена
- Некоторые элементы (например, в iframe) могут требовать дополнительной настройки
- При воспроизведении записей страница должна быть в том же состоянии, что и при записи

## 🐛 Отладка

Для отладки откройте консоль DevTools:
1. Правый клик на панели расширения
2. "Проверить элемент" (Inspect)
3. В консоли будут отображаться логи работы расширения
