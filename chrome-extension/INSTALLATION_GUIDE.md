# QA Test Recorder - Руководство по установке и использованию

## 📦 Как получить расширение

1. Скачать архив - [NEW Chrome-Recorder v4.rar](/.attachments/NEW%20Chrome-Recorder%20v4-3fbe4e90-1db2-4c50-8653-a94b7c4d7c1c.rar)

---

## 🚀 Установка расширения

### Шаг 1: Получите файлы расширения
- Скачайте или распакуйте архив с папкой `NEW Chrome - Recorder`

### Шаг 2: Откройте страницу расширений Chrome
1. Откройте браузер Google Chrome
2. В адресной строке введите: `chrome://extensions/`
3. Нажмите Enter

### Шаг 3: Включите режим разработчика
1. В правом верхнем углу страницы найдите переключатель **"Режим разработчика"** (Developer mode)
2. Включите его (переключатель станет синим)

### Шаг 4: Загрузите расширение
1. Нажмите кнопку **"Загрузить распакованное расширение"** (Load unpacked)
2. В открывшемся окне выберите распакованную папку `NEW Chrome - Recorder`
3. Нажмите **"Выбрать папку"** (Select Folder)

### Шаг 5: Проверьте установку
- Расширение **QA Test Recorder** должно появиться в списке установленных расширений
- Убедитесь, что переключатель расширения включен (синий цвет)

---

## 📖 Как использовать расширение

### Открытие панели рекордера

1. Откройте любую веб-страницу, которую хотите протестировать
2. Нажмите **F12** или **Ctrl+Shift+I** (Windows/Linux) / **Cmd+Option+I** (Mac) для открытия DevTools
3. В верхней панели DevTools найдите вкладку **"QA Recorder"**
4. Если вкладка не видна, нажмите на стрелку `>>` справа и выберите **"QA Recorder"** из списка

### Создание новой записи

1. В панели QA Recorder нажмите кнопку **"Создать запись"**
2. В появившемся диалоговом окне:
   - Введите **название записи** (например, "Тест авторизации")
   - Введите **название тест-сьюта** (например, "Авторизация")
   - Выберите **типы селекторов**, которые будут генерироваться:
     - ✅ **CSS** - стандартные CSS селекторы
     - ✅ **ARIA** - ARIA атрибуты для accessibility
     - ✅ **Text** - поиск по тексту элемента
     - ✅ **XPath** - XPath выражения (рекомендуется для сложных структур)
     - ✅ **Pierce** - Shadow DOM селекторы
3. Нажмите **"Начать запись"**

### Запись действий

После начала записи расширение автоматически фиксирует:

#### 1. Навигация
- Автоматически записывается текущий URL страницы
- Фиксируется размер viewport (размер окна браузера)

#### 2. Клики
- Клики по кнопкам, ссылкам, иконкам
- Выбор радио-кнопок и чекбоксов
- Клики по любым интерактивным элементам

**Специальная обработка для вашего проекта:**

Расширение использует уникальный атрибут **`data-test`** для генерации XPath локаторов. Ключевые особенности:

✅ **Уникальность** - каждый `data-test` уникален и однозначно идентифицирует элемент  
✅ **Интеллектуальное определение** - расширение автоматически находит интерактивный элемент (кнопку, ссылку), а не вложенные текстовые или графические элементы  
✅ **Простота** - генерируется лаконичный XPath без дополнительных условий

**Как это работает:**

1. **Атрибут `data-test` присутствует у всех элементов DOM**
2. **При клике на элемент** расширение поднимается вверх по DOM-дереву для поиска ближайшего интерактивного родителя:
   - `<a>` - ссылки
   - `<button>` - кнопки
   - `<input>`, `<textarea>`, `<select>` - элементы ввода
   - Элементы с `role="button"`, `role="link"`, `role="menuitem"` и т.д.
   - Элементы с атрибутом `onclick`
   - Элементы, чей `data-test` содержит ключевые слова: `Button`, `Link`, `Menu`, `Tab`, `Item`
3. **Генерируется простой XPath** для найденного интерактивного элемента

**Примеры:**

**Кнопка с вложенным текстом:**
```html
<div data-test='Button_Tag_7a9741::0vzkweb::1'>
  <div data-test='::0vzkweb::5'>
    <span data-test='::0vzkweb::6'>Создать заявку</span>
  </div>
</div>
```
При клике на текст "Создать заявку" генерируется XPath:
```xpath
xpath//*[@data-test='Button_Tag_7a9741::0vzkweb::1']
```
Расширение определяет, что клик произошел внутри кнопки, и использует `data-test` именно кнопки.

**Ссылка в меню:**
```html
<div data-test='mainMenu-tasks-allTasks'>
  <a data-test='link::0vzkweb::23'>
    <span data-test='::0vzkweb::24'>Все заявки</span>
  </a>
</div>
```
При клике на текст "Все заявки" генерируется XPath:
```xpath
xpath//*[@data-test='link::0vzkweb::23']
```
Расширение находит элемент `<a>` как ближайший интерактивный родитель.

**Input поле:**
```html
<div data-test='Input_Container_abc123'>
  <input data-test='Input_Field_xyz789' type='text' />
</div>
```
При вводе текста в поле генерируется XPath:
```xpath
xpath//*[@data-test='Input_Field_xyz789']
```
Для input элементов расширение использует сам элемент напрямую.

#### 3. Ввод текста
- Заполнение форм
- Ввод в текстовые поля, textarea
- Фиксируется введенный текст

#### 4. Выбор из списков
- Выбор опций в select/dropdown элементах

#### 5. Изменения (Change events)
- Переключение чекбоксов
- Изменение значений в полях

### Дополнительные инструменты во время записи

#### Пикер элементов (Element Picker)
1. Нажмите кнопку **"Add Asertion"** во время записи
2. Наведите курсор на нужный элемент на странице
3. Элемент подсветится синей рамкой
4. Кликните на элемент
5. В запись добавится шаг **"WaitForElement"** с селекторами этого элемента

**Используйте для:**
- Добавления проверок на наличие элемента
- Ожидания загрузки динамического контента
- Проверки появления модальных окон

### Остановка записи

1. Нажмите кнопку **"Остановить запись"**
2. Все записанные шаги будут сохранены
3. Вы увидите полный список действий в панели

---

## 🔄 Продолжение записи (добавление новых шагов)

Вы можете добавлять новые шаги в уже существующие записи без необходимости создавать новую запись с нуля:

### Как продолжить запись

1. **Откройте запись** - выберите любую сохраненную запись из списка записей
2. **Нажмите "Продолжить запись"** - кнопка находится в правом верхнем углу рядом с кнопками воспроизведения
3. **Расширение переключится в режим записи** с сохранением всех существующих данных:
   - Название записи
   - Название тест-сьюта (SuiteName)
   - Выбранные типы селекторов
   - Все ранее записанные шаги
4. **Выполняйте новые действия** на странице - они будут автоматически добавлены в конец существующей записи
5. **Остановите запись** - нажмите кнопку "Остановить запись" когда закончите
6. **Просмотрите объединенную запись** - автоматически отобразится режим просмотра с обновленной записью

### Важные особенности

#### Автоматический пропуск начальных шагов
При продолжении записи расширение **автоматически пропускает** запись следующих шагов:
- `setViewport` - установка размера окна браузера
- `navigate` - переход на URL страницы

Это происходит потому, что эти шаги уже присутствуют в начале существующей записи и их дублирование не требуется.

#### Сохранение метаданных
При продолжении записи сохраняются все метаданные:
- Название записи (title)
- Название тест-сьюта (SuiteName)
- Типы селекторов (selectedSelectors)
- ID записи

#### Добавление шагов
Новые действия добавляются **в конец** существующего массива шагов. Порядок выполнения сохраняется:
1. Существующие шаги (1, 2, 3, ...)
2. Новые шаги (N+1, N+2, N+3, ...)

### Примеры использования

#### Пример 1: Добавление шагов для проверки результата
```
Исходная запись: "Создание пользователя"
- Шаги 1-5: Заполнение формы создания пользователя
- Шаги 6-7: Отправка формы

Продолжение записи:
- Шаги 8-10: Проверка успешного сообщения
- Шаг 11: Переход на страницу списка пользователей
- Шаг 12: Проверка наличия нового пользователя в списке
```

#### Пример 2: Расширение тест-кейса
```
Исходная запись: "Авторизация в системе"
- Шаги 1-3: Ввод логина и пароля
- Шаг 4: Клик на кнопку "Войти"

Продолжение записи:
- Шаг 5: Ожидание загрузки главной страницы
- Шаги 6-8: Проверка элементов пользовательского меню
- Шаг 9: Выход из системы
```

### Преимущества

✅ **Экономия времени** - не нужно заново записывать начальные шаги

✅ **Гибкость** - можно постепенно наращивать сложность тест-кейса

✅ **Итеративная разработка** - добавляйте проверки по мере необходимости

✅ **Сохранение контекста** - все настройки и метаданные остаются неизменными

---

## 🎬 Работа с записями

### Просмотр шагов записи

После остановки записи вы увидите список всех шагов:
- **Navigate** - переход на URL
- **SetViewport** - установка размера окна
- **Click** - клик по элементу
- **Change** - изменение значения
- **KeyDown** / **KeyUp** - нажатие клавиш
- **WaitForElement** - ожидание элемента

Каждый шаг показывает:
- Тип действия
- Селекторы (CSS, XPath, ARIA, и т.д.)
- Дополнительные параметры (координаты, значения)

### Воспроизведение записи (Replay)

1. Выберите запись из списка
2. Нажмите кнопку **"Replay"**
3. Настройте параметры воспроизведения:
   - **Speed** (скорость): от 0.5x до 2x
   - **Timeout** (таймаут): время ожидания элементов (1000-10000 мс)
4. Нажмите **"Start Replay"**
5. Расширение автоматически выполнит все записанные действия

**Что происходит во время воспроизведения:**
- Страница перезагружается на исходный URL
- Устанавливается размер viewport
- Последовательно выполняются все шаги
- В консоли отображается прогресс
- При ошибках воспроизведение останавливается с описанием проблемы

### Экспорт записи в JSON

1. Выберите запись из списка
2. Нажмите кнопку **"Экспорт JSON"**
3. Файл автоматически скачается с названием `recording-[название]-[дата].json`

**Формат JSON совместим с Chrome DevTools Recorder:**
```json
{
  "title": "Название записи",
  "SuiteName": "Название тест-сьюта",
  "selectorAttribute": "data-testid",
  "steps": [
    {
      "type": "navigate",
      "url": "https://example.com"
    },
    {
      "type": "click",
      "selectors": [
        ["[data-testid='button']"],
        ["xpath//div[@data-testid='button' and .//span[text()='Кнопка']]"]
      ],
      "offsetX": 10,
      "offsetY": 20
    }
  ]
}
```


### Удаление записи

1. Выберите запись из списка
2. Нажмите кнопку **"Удалить"**
3. Подтвердите удаление
4. Запись будет удалена из локального хранилища

### Просмотр кода шагов

1. Выберите запись из списка
2. Нажмите кнопку **"Show Code"**
3. Откроется модальное окно с JSON представлением всех шагов
4. Вы можете скопировать код для дальнейшего использования

---

## ⚙️ Настройки воспроизведения

### Speed (Скорость)
- **0.5x** - медленное воспроизведение (для отладки)
- **1x** - нормальная скорость (по умолчанию)
- **1.5x** - ускоренное воспроизведение
- **2x** - быстрое воспроизведение

### Timeout (Таймаут ожидания)
- **1000 мс** - быстрый таймаут (для быстрых страниц)
- **3000 мс** - стандартный таймаут (по умолчанию)
- **5000 мс** - увеличенный таймаут (для медленных страниц)
- **10000 мс** - максимальный таймаут (для очень медленных операций)

---

## 🎯 Типы селекторов

### CSS Selectors
Стандартные CSS селекторы:
```css
[data-testid='button']
.class-name
#element-id
```

### ARIA Selectors
Селекторы по ARIA атрибутам (для accessibility):
```
aria/Button Name
aria/Click me[role="button"]
```

### Text Selectors
Поиск по видимому тексту:
```
text/Создать заявку
```

### XPath Selectors
XPath выражения для сложных структур:
```xpath
//div[@data-testid='Button_Tag_7a9741' and .//span[text()='Создать заявку']]
xpath//*[@data-testid='ListItem_ListItemRoot_9f8b7c' and .//*[text()='Андрей']]
```

### Pierce Selectors
Для работы с Shadow DOM:
```
pierce/.shadow-element
```

---

## 💡 Лучшие практики

### 1. Выбор селекторов
- ✅ Используйте **XPath** для элементов с `data-testid` и вложенным текстом
- ✅ Включайте несколько типов селекторов для надежности
- ✅ Предпочитайте `data-testid` атрибуты для стабильности тестов

### 2. Создание записей
- 📝 Давайте записям понятные названия (например, "Создание пользователя", "Успешная авторизация")
- 🎯 Разбивайте сложные сценарии на несколько коротких записей
- ⏸️ Делайте паузы между действиями при записи на медленных страницах

### 3. Воспроизведение
- ⏱️ Увеличивайте timeout для медленных страниц или асинхронных операций
- 🔄 Используйте "WaitForElement" перед взаимодействием с динамическим контентом
- 🧪 Тестируйте записи на чистом состоянии страницы (очищенные cookies, новая сессия)

### 4. Работа с динамическим контентом
- Используйте **Element Picker** для добавления проверок загрузки
- Добавляйте **WaitForElement** перед взаимодействием с элементами, которые появляются после загрузки данных

### 5. Использование продолжения записи
- 🔄 Используйте **"Продолжить запись"** для добавления проверок результата
- 🎯 Постепенно наращивайте сложность тест-кейса
- 📝 Добавляйте новые шаги по мере обнаружения необходимых проверок

### 6. Отладка
- 🔍 Открывайте консоль браузера для просмотра логов воспроизведения
- 📊 Используйте медленную скорость (0.5x) для наблюдения за процессом
- 🛠️ Проверяйте селекторы через "Show Code" если шаг не выполняется

---

## 🆕 Новый функционал

### Улучшенная обработка кликов

#### Поддержка элементов `<li>`
Элементы `<li>` теперь корректно обрабатываются при воспроизведении. Расширение автоматически находит вложенные интерактивные элементы (`<a>`, `<button>`, элементы с `role="menuitem"`, `role="option"`, `role="tab"`) и выполняет клик по ним.

**Пример:**
```html
<ul>
  <li data-test='menu-item-1'>
    <a href='/page1'>Ссылка 1</a>
  </li>
  <li data-test='menu-item-2'>
    <button>Кнопка 2</button>
  </li>
</ul>
```
При воспроизведении клика по `<li>` расширение автоматически найдет и кликнет по вложенной ссылке или кнопке.

#### Корректная обработка опций в модальных окнах
Элементы с `data-test="::..."` и коротким текстом (до 32 символов) — такие как названия месяцев, дни недели, опции в списках — теперь кликаются напрямую, без перенаправления на родительский элемент.

**Пример структуры месяцев:**
```html
<div data-test='Body_ModalBody_65770c::1haggwj::0'>
  <div data-test='::1haggwj::1'>Январь</div>
  <div data-test='::1haggwj::2'>Февраль</div>
  <div data-test='::1haggwj::3'>Март</div>
  <!-- ... -->
</div>
```

**Пример структуры дней недели:**
```html
<div data-test='::166ban7::2'>
  <div data-test='::166ban7::3'>ПН</div>
  <div data-test='::166ban7::4'>ВТ</div>
  <div data-test='::166ban7::5'>СР</div>
  <!-- ... -->
</div>
```

При клике на "Апрель" или "ВТ" расширение теперь корректно выполнит клик именно по этому элементу.

#### Улучшенная надежность первого клика
- Двухэтапная симуляция движения мыши (`mouseMoved`) перед кликом для активации hover-состояний и динамических меню
- Ожидание до 900мс перед кликом для проверки, что элемент не перекрыт анимациями или оверлеями
- Использование `elementFromPoint` для проверки доступности целевого элемента

#### Валидация кликов
После выполнения клика расширение проверяет, произошло ли изменение в DOM (мутация, смена URL, изменение activeElement). Если клик не привел к изменениям UI, шаг помечается как ошибка с сообщением "Клик не изменил UI".

### Воспроизведение папок (Bulk Playback)

Расширение поддерживает последовательное воспроизведение всех записей в папке:

1. **Статус-бар прогресса** — отображается во время воспроизведения папки
2. **Блокировка создания записей** — кнопка "Создать запись" отключена во время bulk-воспроизведения
3. **Автоматическая навигация** — UI автоматически переходит в папку, которая воспроизводится
4. **Визуальная индикация результатов**:
   - 🟢 Зеленый фон — запись выполнена успешно
   - 🔴 Красный фон — ошибка при выполнении записи

### Структура JSON записи

#### Разделение основных шагов и проверок
Шаги `waitForElement` теперь хранятся в отдельном массиве `checkSteps`, а не в основном массиве `steps`:

```json
{
  "title": "Название записи",
  "SuiteName": "Название тест-сьюта",
  "steps": [
    { "type": "navigate", "url": "https://example.com" },
    { "type": "click", "selectors": [...] }
  ],
  "checkSteps": [
    { "type": "waitForElement", "selectors": [...] }
  ]
}
```

### Улучшенная обработка ввода текста

#### Сброс pending-ввода перед кликом
Перед каждым кликом расширение автоматически завершает (flush) все незавершенные события ввода текста. Это критически важно для корректной работы форм — текст, введенный в `textarea` или `input`, гарантированно сохраняется в DOM перед нажатием кнопок "Сохранить" или "Отправить".

#### Поддержка сложных редакторов
Для редакторов типа Draft.js (например, с атрибутом `aria-label='rdw-editor'`) используется `Input.insertText` для корректного обновления состояния фреймворка.

---

## ⚠️ Важные замечания

### Ограничения
- ❌ Расширение работает только при открытых DevTools
- ❌ Страница должна быть полностью загружена перед началом записи
- ❌ Некоторые iframe могут требовать дополнительной настройки
- ❌ Drag & Drop операции могут записываться некорректно

### Требования к странице при воспроизведении
- ✅ Страница должна быть в том же состоянии, что и при записи
- ✅ Все элементы должны быть доступны (не скрыты, не заблокированы)
- ✅ URL должен быть доступен и загружаться корректно

### Безопасность
- 🔒 Все записи хранятся локально в браузере
- 🔒 Данные не передаются на внешние серверы
- 🔒 Будьте осторожны при экспорте записей с конфиденциальными данными (пароли, токены)

---

## 🐛 Отладка и решение проблем

### Расширение не отображается в DevTools
1. Убедитесь, что расширение установлено и включено в `chrome://extensions/`
2. Закройте и откройте DevTools заново
3. Обновите страницу с расширением

### Запись не начинается
1. Проверьте, что DevTools открыты
2. Убедитесь, что страница полностью загружена
3. Проверьте консоль на наличие ошибок
4. Попробуйте обновить страницу и начать запись заново

### Воспроизведение не работает
1. Проверьте, что URL страницы доступен
2. Убедитесь, что страница в том же состоянии, что при записи
3. Увеличьте timeout в настройках воспроизведения
4. Проверьте, что селекторы элементов не изменились

### Элементы не находятся при воспроизведении
1. Откройте "Show Code" и проверьте селекторы
2. Попробуйте вручную найти элемент в консоли: `document.querySelector('[data-testid="..."]')`
3. Добавьте WaitForElement шаги перед проблемными действиями
4. Проверьте, что элемент не находится в iframe


---

**Версия расширения:** 1.0.3  
**Последнее обновление документации:** 2025-01-04
